# Cycle 3 pre-execution characterization method

This is an experiment-only, non-installed runner. It imports no decoder, helper, fixture generator, expected output, product host, or excluded runtime until the separately accepted runner gate has passed. No decoder has run while this method is being prepared or validated.

Before each launch the parent verifies binding v3: it names corrected accepted fixture candidate `3af6b9f80dd52611e14acca407707d1da7b8d9ac` / tree `14c010153ea5542617e601e4126047c561a48483`, semantic-fixture SHA-256 `34d2a114b653dc1a5d2fc9f253457e11e6b1acd4bc10d38ffa98d84bdd93d094`, cap SHA-256 `6347a04f44f02179e73fd9178fce4daad227d06c9a7a4ac2c12e0b0669f60d3d`, and fixture-manifest SHA-256 `71c64b2c80142cf5d535e03f93f9d3ce66ada120582115fbf403f3621f97c5b9`. This correction follows fixture verification `3a29e4684af6a4c8e53aa4f83f9692ddebab5670`, accumulation `efcc7759f4af89eb438b2b8611e250d0962fc8c6`, and acceptance `972322116b737d2fcad37e46d23116b031841359`; it rebinds the runner only and reports no decoder run. Its closed source map covers this method, all tool sources/configuration, the six accepted fixture artifacts, and the exact nine snapshot bytes. It rejects missing/extra paths, traversal, symlinks, substitutions, source digest changes, and every stale ordered path-NUL-digest-LF decoder-set hash. Run every parent command with `PYTHONDONTWRITEBYTECODE=1`; `python -m tools.cycle3_characterize.binding --check` then recomputes only non-self hashes and starts no worker.

The future child is exactly Python 3.12 `-I -B worker.py`, working in `tools/cycle3_characterize`, with a minimal environment and one inherited write-only framed-JSON protocol FD. Parent stdout/stderr are separately drained and bounded. The worker sets the exact recursion and 64-GiB address-space cap before parsing, and the parent owns deadline, terminate, kill, close, and reap. Every bad frame, cap breach, timeout, bad result, or child failure discards the whole result. The immutable profile has exactly the reviewed 17 positive caps.

Only the private `_orchestration_execute_approved_snapshot` entrypoint can make the post-gate call; `characterize` remains unavailable as a public host surface. It accepts no module, path, import, or probe selector: only a fixed decoder ID (`uart`, `spi`, or `i2c`), that decoder's provenance-derived ordered file list, a positive samplerate, a fixed-length logical-to-physical bit map (0--255, where 255 means unavailable), bounded packed samples, and closed decoder options. Probe mode is a different parent-token-guarded request form and is inaccessible from characterization.

After the independent gate, the worker rereads the exact pinned bytes, builds only the fixed `sigrokdecode`, `common.srdhelper`, and selected decoder module graph in memory, and runs the minimal API-v3 lifecycle: `HasChannel`, `Wait`, `Register`, `Put`, `samplenum`/`matched`, defaults/options, metadata, start, and decode. `Wait` deterministically searches samples and `Put` records half-open intervals. Raw observations are canonical JSON records in emission order with integer sample coordinates, output ID, and recursively canonicalized values; the method retains deterministic input-sample, request-byte, record, encoded/decoded, text/binary/diagnostic, and retained-result counts.

For each approved snapshot, record machine model, macOS version, Python 3.12 build, CPU architecture, resource-cap values, input/request/count totals, parent launch/reap/total nanoseconds, child load/import/decode nanoseconds, parent retained memory, child address-space result, recursion result, cancellation/terminate/kill/reap latency, and raw canonical result. The runner's immutable success result supplies launch/reap/total timing; its private cleanup evidence supplies total and terminate-to-reap/kill-to-reap timing for hostile cases. Run one unrecorded warm-up followed by five independent repetitions in the same recorded machine/Python environment; retain every raw result and report min/median/max timing without averaging away failures. A timeout or cap breach is an observation, never a passing baseline. This document defines the procedure only: it reports no baseline, threshold, decoder result, or execution claim.
