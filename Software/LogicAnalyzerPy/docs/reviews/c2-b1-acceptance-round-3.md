# C2-B1 independent acceptance — round 3

- Lane / identity: acceptance / `cycle2-acceptance`
- Implementor identity: `cycle2-implementor-replacement-1`
- Verifier identity: `cycle2-verifier`
- Reviewed candidate commit: `e266d88815b0dabdd1397cbfcf3e1187bf8dfc24`
- Reviewed candidate tree: `9ec8c3ef73e47c940fd686656708cfaa212d9058`
- Governing platform scope: macOS only
- Review date: 2026-08-23 America/Los_Angeles
- Prior B1 verdict transfer: none
- Verdict: `pass`

## Candidate and authority identity

The assigned commit exists and resolves to the assigned tree. The
operator-authorized macOS-only platform revision verification and acceptance
records both pass for this exact candidate. The revision changes exactly the
authorized goal, orchestration contract, batch contract, single workflow,
README support statement, and append-only progress record. It preserves prior
discovery, review, acceptance, CI, and evidence history and changes no firmware,
C# application, Python product implementation, test, dependency lock, built
asset, or excluded Cycle 3 surface.

Independent B1 verification record
`Software/LogicAnalyzerPy/docs/reviews/c2-b1-verification-round-18.md` has SHA-256
`28b3c6e8349982e3faec8517572cedc42e7086da12a3c3c5f6858d8149f08feb`, names the
same candidate and tree, reports `pass`, and has no open finding. Earlier B1
acceptance records remain immutable historical records; their verdicts are not
transferred to this revised-contract round.

## Complete accumulated gate

Fresh clean Python 3.12 core and web environments installed their respective
hash-locked requirements, installed the source without build isolation or
dependency resolution, and passed both `pip check` gates. Ruff passed; mypy
passed over 19 source files; 232 non-hardware tests passed; CLI help passed.
The focused one-overall-deadline recovery and B1 foundation run passed eight
tests. The timeout fixture uses an injected monotonic clock and continues to
distinguish one shared finite deadline from a renewed per-byte budget.

The evidence-validator probe rejected all 10 hostile values, including literal
capability, authorization, cookie, API-key/password, serial-path, and loopback-
URL forms across nested and free-text fields. It accepted all four benign
security statements and the valid baseline manifest. Recursive field
strictness, role identity, digest, timestamp, machine-local-value, and secret
checks remain active without preventing ordinary evidence prose.

With pinned Node 22.14.0, locked npm installation, lint, typecheck, unit test,
production build, asset-drift check, and installed-Chromium Playwright
production smoke all passed. The browser evidence exercises committed local
assets, readiness, the HttpOnly/SameSite capability boundary, protected orderly
shutdown, and console token non-disclosure without a Node development server or
serial dependency.

The verifier's optional artificial-minimal-`PATH` defense-in-depth probe was
externally interrupted and is explicitly not credited. It is not a governing
B1 command and does not replace or qualify the completed required full,
focused, production-browser, or hosted gates.

## Hosted macOS evidence

Public hosted run `32624529709` identifies exact commit
`e266d88815b0dabdd1397cbfcf3e1187bf8dfc24`, tree
`9ec8c3ef73e47c940fd686656708cfaa212d9058`, and completed with `success`. Its
jobs endpoint reports exactly one job. macOS `validate` job `97157701532`
identifies the exact commit and completed with `success`. This matches the
current macOS-only workflow and creates neither an omitted active platform gate
nor a Linux support claim.

## Full B1 contract and scope audit

The current contracts retain all fourteen stopping conditions and all six
batches. B1 provides the required Python/React foundation, Python optional web
extra and complete runtime lock, pinned Node frontend and lock, deterministic
committed production assets, inert loopback-only shell, canonical Host and
Origin checks, capability-cookie boundary, request bounds, deterministic
lifecycle, missing-extra diagnostic, strict evidence schema/validator, exact
canonical commands, single hosted macOS workflow, and Cycle 1 non-hardware
regression coverage.

Historical Linux statements remain unchanged and are explicitly identified as
past requirements or results, not current support. The active contracts,
workflow, README, and completion handoff consistently state macOS-only support.
No unrelated validation, security, physical, performance, rollback, evidence,
or prohibited-action requirement was removed by the platform revision.

Wider codec/model work, replay-schema-2 production I/O, capture API, waveform
viewer behavior, hardware, performance thresholds, native live integration,
and final proof remain assigned to C2-B2 through C2-B6. Firmware, bootloader,
Wi-Fi, persistent device state, public serving, `.lac`, .NET, Qt/Electron,
packaging/publication, C# replacement, and Cycle 3 remain prohibited or out of
scope. None occurred.

## Findings and disposition

No acceptance finding is open. The exact candidate satisfies the complete
pre-manifest C2-B1 gate under the operator-approved macOS-only contracts.

## Manifest and checkpoint handoff

The C2-B1 manifest is correctly absent at acceptance time. The orchestrator may
now atomically create only
`Software/LogicAnalyzerPy/testdata/evidence/c2-b1.json`. It must identify this
exact candidate and tree; the three distinct role identities; the exact local,
browser, and hosted command results; hosted run `32624529709` and sole macOS job
`97157701532`; source provenance; findings and dispositions; current stopping-
condition coverage; limitations and deferred work; and these SHA-256 identities:

- `requirements-dev.lock`:
  `6f78ec8f0219b919c44ed9b9f1d30c2337fcce7c911b8d3dc4c13edb707a6575`;
- `requirements-web.lock`:
  `6f78ec8f0219b919c44ed9b9f1d30c2337fcce7c911b8d3dc4c13edb707a6575`;
- `web/package-lock.json`:
  `d388a2668b3f65edeb62ee99da3ad37ffc56ab1ca3439b6d418a6bbe92e838d9`;
- `web/production-assets.json`:
  `1d1b44794eed6f288f1140e67c1520d847fb3263718672cf743ff70b9da0ece9`.

The manifest must pass the candidate's Cycle 2 validator and be committed
before the orchestrator appends the C2-B1 checkpoint and marks the batch
complete. No C2-B2 through C2-B6 manifest may be created early, and historical
Linux evidence must not be represented as a current support requirement.

## Final state

Verdict is `pass`; no finding remains open. C2-B1 may advance only through the
orchestrator-owned atomic manifest validation/commit and subsequent checkpoint
append required by the governing procedure.
