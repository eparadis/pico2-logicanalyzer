# C3-B1 final integration candidate — round 2

## Status and subject

This document and
`testdata/decoders/cycle3/b1-final-candidate-round-2.json` form the corrected
final C3-B1 integration candidate assigned at
`560029256b5e7c26efaacf6576116f2833637ece`. Status is
`candidate-not-accepted`. No internal candidate pass transfers to this final
candidate, and no B1 manifest, checkpoint, or B2 authority exists yet.

Round-1 candidate `72e7b35` and failed verification `a9627e6` remain immutable.
Finding `C3B1-FV001` required explicit ordered implementation histories rather
than unbound preservation booleans. This round changes only those histories
and correction provenance; every evidence value, digest, command, environment,
status, finding, applicability rule, exclusion, and authority boundary remains
unchanged.

The machine-readable binder is authoritative for exact commits, trees,
digests, role identities, environment qualification, frozen commands,
historical invalidations, and exclusions. It binds governing commit/tree
`ea83a403bb6f644d33069d3bebd41d5f1e0a60c0` /
`d9fb09345ab85580cd8c877745262d1b481e366f`.

## Complete internal identity map

The corrected accepted fixture implementation history is, in order:
`c3-b1-semantic-fixture-implementor-1`, then
`c3-b1-semantic-fixture-implementor-replacement-1`, `-2`, `-3`, `-4`, `-5`,
and final owner `-6`. It was independently verified by
`c3-b1-semantic-fixture-verifier-1` and accepted by
`c3-b1-semantic-fixture-acceptance-1`.

The corrected accepted pre-execution runner implementation history is, in
order: `c3-b1-pre-execution-runner-implementor-1`, then
`c3-b1-pre-execution-runner-implementor-replacement-1`, `-2`, and final owner
`-3`. It was independently verified by
`c3-b1-pre-execution-runner-verifier-1` and accepted by
`c3-b1-pre-execution-runner-acceptance-1`. All superseded candidates/findings
remain immutable history; the binder does not collapse earlier identities into
the final owners or transfer their results.

Project/license disposition is independently bound to
`c3-b1-project-license-reviewer-1`. The primary orchestrator owns only this
integration binder, accumulated validation, later manifest assembly, progress,
and checkpoint sequencing; it is not one of the six internal evidence roles.

## Integrated evidence

The binder closes these ordered gates without altering their subjects:

1. exact nine-file provenance, notices, import closure, and engineering
   project/license disposition;
2. corrected semantic-fixture candidate `7c57a347`, with all 63 timelines,
   1,058 waits, 1,049 records, 165 option rows, typed vectors/goldens, 17
   experiment caps, verification, accumulated validation, and acceptance;
3. corrected cache-free runner candidate `48d139f`, with fixed isolated
   `-I -B` launch, pinned imports, all 17 experiment-cap paths, cleanup,
   non-decoder verification, accumulated validation, and acceptance;
4. immutable raw candidate `397981a`, its complete reproduction,
   accumulated validation, acceptance, environment and timing/RSS discrepancy
   record;
5. corrected 29-limit proposal `395646f`, finding `C3B1-TPV001` history,
   fresh verification, accumulated validation, acceptance, and explicit
   operator approval committed at `b6d9040`.

Approved values remain defined only by the exact proposal and approval record.
This binder repeats no independent alternative that could silently diverge.
Every approved value is inclusive and its first rejected integer is value + 1.

## Frozen validation and limitations

The command binder freezes repository-local Python 3.12 lint, strict type,
deterministic fixture/binding, applicable non-hardware, evidence, asset, and
installed-help classes plus exact Node 22 lint/type/unit/build/browser/
performance classes. The three explicitly deselected historical node IDs and
their replacement proofs remain transparent and bound to the proposal
accumulated record.

The candidate makes no product enforcement, public API, containment, package,
distribution, hardware, non-macOS, checkpoint, B2, or Cycle 4 claim. Packaging
the snapshots remains subject to the exact project/license disposition and its
rights-holder/counsel limitation. All historical failed candidates and
discarded raw runs remain evidence, not inputs to accepted numeric values.

Fresh final B1 verification, complete accumulated validation, independent
acceptance, atomic manifest verification, manifest commit, and checkpoint are
still mandatory in that order.
