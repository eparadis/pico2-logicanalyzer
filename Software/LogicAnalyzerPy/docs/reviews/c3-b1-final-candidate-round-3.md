# C3-B1 replacement final integration candidate — round 3

## Status and immutable subject

This document and
`testdata/decoders/cycle3/b1-final-candidate-round-3.json` form the replacement
final C3-B1 integration candidate assigned at
`70b6d88bb7a2bde703fdc69f8375cab7658f4100`. Status is
`candidate-not-accepted`. No internal or former final-B1 pass transfers to this
candidate, and the superseded B1 manifest/checkpoint supplies no B2 authority.

The candidate binds governing contract commit/tree
`ea83a403bb6f644d33069d3bebd41d5f1e0a60c0` /
`d9fb09345ab85580cd8c877745262d1b481e366f`. It adds no product, fixture,
runner, raw, threshold, lock, workflow, manifest, checkpoint, or B2 byte.

## Correction history and role map

Finding `C3B2-IF001` invalidated the former final candidate `1f87932`, B1
manifest commit `c34a251`, and checkpoint `06ca562` after B2 exposed an
authoritative incomplete-UART fixture error. The corrected fixture candidate
is `a98d328aab92f3dad66988fb70cc567946dc89d0`, tree
`450aadcf26bd6c74860872bfbf0a0a1b32d94f3b`. Its expected wait trace and six
pre-end-of-input emissions were independently derived from declarative input
and inert frozen source, never from decoder, host, or raw runtime output.

The complete semantic-fixture implementation history is, in order,
`c3-b1-semantic-fixture-implementor-1`, replacements 1 through 6, and final
correction owner `c3-b1-semantic-fixture-implementor-replacement-7`. The
corrected candidate was verified by
`c3-b1-semantic-fixture-correction-verifier-1` and accepted by
`c3-b1-semantic-fixture-correction-acceptance-1`.

The complete pre-execution-runner implementation history remains, in order,
`c3-b1-pre-execution-runner-implementor-1`, replacements 1 and 2, and final
owner `c3-b1-pre-execution-runner-implementor-replacement-3`. Its corrected
fixture rebinding was verified by
`c3-b1-pre-execution-runner-rebinding-verifier-1` and accepted by
`c3-b1-pre-execution-runner-rebinding-acceptance-1`.

All six current internal identities are pairwise distinct. The primary
orchestrator owns only integration, accumulation, approval routing, later
manifest assembly, progress, and checkpoint sequencing. The complete prior
`C3B1-FV001` ordered-history correction remains explicit; no identity is
collapsed and no result transfers across roles or candidates.

## Integrated evidence

The authoritative JSON binder closes these ordered internal gates without
changing their immutable subjects:

1. exact nine-file provenance, closed imports, notices, and the existing
   engineering project/license disposition;
2. corrected semantic-fixture candidate `a98d328`, including all 63 timelines,
   corrected fixture/cap/manifest digests, fresh verification, complete
   accumulation, and fresh acceptance;
3. fixture-rebound pre-execution runner `ca0ee75`, with exact closed binding,
   fixed isolated `-I -B` launch, all 17 experiment-cap paths, cleanup,
   non-decoder verification, complete accumulation, and fresh acceptance;
4. fresh immutable raw candidate `2ae1e43`, its corrected independent
   reproduction, complete accumulation, acceptance, and exact environment,
   request, observation, hostile-observation, and manifest digests;
5. exact 29-limit round-3 proposal `b260d9e`, fresh verification, complete
   accumulation, acceptance, and renewed explicit operator approval committed
   at `63fdfee`.

The corrected raw maximum for successful parent total is 123,103,531 ns.
Twenty-eight approved values remain numerically unchanged from round 2; only
`successful_parent_total_ns` changes from 900,000,000 to 1,000,000,000 ns.
Every approved value is inclusive and its first rejected integer is value + 1.
The maximum approved SPI word size remains eight bits. The round-2 proposal and
approval are explicitly superseded and confer no authority.

The binder preserves closed findings `C3B1-RI011`, `C3B1-RI013`,
`C3B1-TPV001`, `C3B1-FV001`, `C3B1-MP001`, `C3B1-MV001`, `C3B1-MV002`,
`C3B2-IF001`, and `C3B1-RV001`, together with superseded fixture, runner, raw,
threshold, final-candidate, manifest, and checkpoint history.

## Frozen validation and applicability

The command binder freezes repository-local Python 3.12 dependency, Ruff,
strict type, deterministic fixture/binding, applicable non-hardware, evidence,
asset, and installed-help classes plus exact Node 22 lint, type, unit, build,
browser, and performance classes.

The latest complete accumulated inventory passed 635 applicable tests with 149
inherited warnings. It transparently deselected two exact historical JSON
namespace assertions and ignored four downstream or superseded exact-subject
modules: the old final candidate, old runner candidate, round-1 proposal, and
round-2 proposal verifiers. A targeted audit reproduced exactly 44 historical
failures and 45 still-passing negative checks. These are ordered invalidations,
not waivers; fresh replacement final verification and accumulation must repeat
and update the applicable inventory after their own verifier test exists.

## Limitations and next gates

The project/license review remains an engineering disposition, not legal
advice. Its rights-holder or qualified-counsel limitation continues to prohibit
package build, conveyance, or distribution absent confirmation.

This candidate makes no product enforcement, public decode API, package,
distribution, hardware, non-macOS, B2, or Cycle 4 claim. The preserved
untracked B2 work is neither an input nor evidence.

Fresh replacement final-B1 verification, complete accumulated validation,
fresh independent acceptance, atomic replacement-manifest assembly, distinct
manifest verification, joint manifest/verification commit, and a replacement
B1 checkpoint are mandatory in that order. Only that checkpoint may resume B2.
