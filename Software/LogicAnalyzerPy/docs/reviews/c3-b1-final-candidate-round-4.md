# C3-B1 replacement final integration candidate — round 4

## Status and immutable subject

This document and
`testdata/decoders/cycle3/b1-final-candidate-round-4.json` form the immutable
replacement final C3-B1 integration candidate assigned at
`212fc3327715bd31a3bc3cd59b10d746ec4c7108`. Its status is exactly
`candidate-not-accepted`. No internal, former final-B1, manifest, checkpoint,
or B2 verdict transfers to this candidate.

The candidate binds governing contract commit/tree
`ea83a403bb6f644d33069d3bebd41d5f1e0a60c0` /
`d9fb09345ab85580cd8c877745262d1b481e366f`. It adds no product, fixture,
runner, raw, threshold, lock, workflow, manifest, checkpoint, B2, or Cycle 4
byte. The three preserved untracked B2 paths were neither inspected nor used.

## Ordered correction history and role map

`C3B2-IF001` invalidated the former final candidate `1f87932`, manifest
`c34a251`, and checkpoint `06ca562`. Round-3 final candidate `4a7dc72` rebound
the first correction but was subsequently invalidated by `C3B2-IF002`, which
found that the unshifted-I2C binary oracle disagreed with the independently
derived Python and annotation semantics. The corrected fixture candidate is
`3af6b9f80dd52611e14acca407707d1da7b8d9ac`, tree
`14c010153ea5542617e601e4126047c561a48483`; exactly one generated semantic
record changed, from shifted binary `0x50` to unshifted binary `0xA0`.

The complete semantic-fixture implementation history remains ordered as
`c3-b1-semantic-fixture-implementor-1`, replacements 1 through 7, and current
owner `c3-b1-semantic-fixture-implementor-replacement-8`. Fresh verifier
`c3-b1-semantic-fixture-correction-verifier-2` and acceptor
`c3-b1-semantic-fixture-correction-acceptance-2` are distinct.

The complete runner implementation history remains ordered as
`c3-b1-pre-execution-runner-implementor-1`, replacements 1 through 3, and
current owner `c3-b1-pre-execution-runner-implementor-replacement-4`.
`C3B1-RA001` preserves the interrupted unfiltered accumulated command as a
possible pre-acceptance launch with no result transfer. `C3B1-RV002` and
`C3B1-RV003` preserve the two non-portable verifier attempts with no verdict
transfer. Only verifier-5 (`e95706f`) and acceptor-3 (`01f059c`) supply the
current runner pass. The six current internal identities are pairwise distinct.

The earlier ordered-history correction `C3B1-FV001` and closed findings
`C3B1-RI011`, `C3B1-RI013`, `C3B1-TPV001`, `C3B1-MP001`, `C3B1-MV001`,
`C3B1-MV002`, and `C3B1-RV001` remain explicit immutable history.

## Integrated accepted evidence

The machine-readable binder closes these inputs without changing them:

1. project/license disposition `538a4aa`, with its rights-holder or counsel
   limitation;
2. corrected 63-timeline fixture candidate `3af6b9f`, verification `3a29e46`,
   accumulation `efcc775`, and acceptance `9723221`;
3. replacement runner candidate `380d5dd`, current verification `e95706f`,
   accumulation `e1e8bab`, and acceptance `01f059c`, retaining closed imports,
   17 caps, isolated launch, containment, termination, descriptor closure,
   reap, and recovery boundaries;
4. fresh raw candidate `d0d9dbe`, verification `1739736`, accumulation
   `7295e4d`, and acceptance `72168f4`; and
5. round-4 proposal `b680aa5`, verification `46d57e4`, accumulation `62b67fe`,
   acceptance `f28643b`, and exact operator approval `afd4585`.

The raw candidate's canonical artifact SHA-256 values are environment
`d0c038a1…aa6f`, requests `fe5b3452…f94c`, observations `ba2c7bd1…29ee`,
hostile observations `af86b376…d91`, and manifest `5d08929c…61b8`. Its one
complete collection used 486 launches. Four earlier pre-write attempts used
0, 6, 384, and 457 launches; all 847 outputs, timings, counts, and authority
were discarded. Independent verification and accumulation each completed a
separate 486-launch reproduction before this binder was authored.

The exact approved proposal and approval-record SHA-256 values are
`98e48bb1882104d893a1d4d85659845ff20054f9b52f0b09fdf73efad2547e3c` and
`ff1ba9665174b0912adea9eb4682d1a9e6839c6ec9b5efce073a9b055303aba6`.
All 29 values are inclusive; the first rejected integer is value + 1. The
hard limits are 65,536 input samples, 4,194,304 request bytes, 5,000 ms wall
deadline, 250 ms terminate grace, 68,719,476,736 worker address-space bytes,
320 recursion frames, 16,384 output records, 2,097,152 encoded bytes,
2,097,152 decoded bytes, 65,536 stdout bytes, 65,536 stderr bytes, 8,192
diagnostic bytes, 524,288 text bytes, 262,144 binary bytes, depth 32, 16,384
nested items, 8,388,608 retained-result bytes, and maximum SPI word size 8.
The regression ceilings are 134,217,728 worker peak-RSS bytes, 33,554,432
parent-retained-growth bytes, 1,300,000,000 successful-parent-total ns,
50,000,000 launch ns, 50,000,000 reap ns, 100,000,000 each for child load,
import, and decode, 5,500,000,000 timeout-cleanup-total ns, 300,000,000
terminate-to-reap ns, and 50,000,000 kill-to-reap ns.

Exactly 26 values remain unchanged from round 3. The three changed values are
`parent_retained_growth_bytes` 16,777,216 to 33,554,432,
`successful_parent_total_ns` 1,000,000,000 to 1,300,000,000, and `launch_ns`
100,000,000 to 50,000,000. Round-3 proposal `b260d9e` and approval `63fdfee`
are superseded and transfer no authority.

## Frozen validation and applicability

The binder freezes repository-local Python 3.12 dependency, Ruff, strict
product and characterization type, deterministic fixture and binding,
installed help, six Cycle-2 evidence, applicable non-hardware, asset, and
exact Node 22 lint, type, unit, build, browser, and performance command
classes. Every current Python gate uses a pre-enumerated include-only path set;
ordered downstream and superseded exclusions are invalidations, never waivers.

The latest complete threshold accumulated inventory classified 56 modules as
41 included and 15 excluded, collected 608 tests, and passed 602 with six
inherited classified skips and 106 warnings. It included the fresh 486-launch
raw reproduction and 30 proposal-verifier tests. Fresh final verification must
construct a new static verifier, and root accumulation must recompute the exact
inventory including that verifier before acceptance. Historical counts are
context, not a transferred final-candidate pass.

## Limitations and mandatory next gates

The project/license disposition is engineering review, not legal advice.
Package build, conveyance, publication, and distribution remain prohibited
without rights-holder or qualified-counsel confirmation. The thresholds are
exact-candidate regression gates for the characterized macOS environment, not
real-time or cross-platform guarantees.

This candidate makes no product-enforcement, public decode API, package,
hardware, non-macOS, B2, or Cycle 4 claim. Fresh replacement round-4 final-B1
verification, complete root accumulated validation, fresh independent
acceptance, atomic replacement-manifest assembly, distinct manifest
verification, joint manifest/verifier commit, and a replacement B1 checkpoint
are mandatory in that order. Only that checkpoint may resume B2.
