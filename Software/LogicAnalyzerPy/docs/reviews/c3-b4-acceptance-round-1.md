# C3-B4 independent acceptance — round 1

## Identity, subject, and verdict

- Acceptance identity: `c3-b4-public-acceptor-1`
  (`/root/c3_b4_accept_r1`), fresh and distinct from the B4 implementor,
  verifier, accumulated-validation owner, and planned manifest verifier.
- Timestamp: `2026-09-02T09:27:10Z`.
- Governing contract commit/tree:
  `ea83a403bb6f644d33069d3bebd41d5f1e0a60c0` /
  `d9fb09345ab85580cd8c877745262d1b481e366f`.
- Accepted B3 prerequisite checkpoint/tree:
  `233b3b0667add740cee96ce3c9ee266c8a639067` /
  `0d3709a861bf79f002246836a582b87677e29b15`.
- Exact B4 product/test/workflow candidate/tree:
  `921f8984b45d78d544494733c9fcd043a788f866` /
  `0b0ea4529e6c2279cdd5b6b766377ea5e75a73e7`.
- Independent verifier evidence commit/tree:
  `5c3427f0a176ba4db8f48b17521d9482f42ead8b` /
  `43f1b950ade7d2ebb1ce82941c71be8ec23e7171`.
- Assigned acceptance HEAD/tree, containing only the later accumulated record:
  `63af13bfd2bdec2af7eb73d6d2a4b11bec5f1559` /
  `1b0a46f60d9fdf3acb4d3fc37a05669ae5720ea1`.
- Environment: macOS 15.7.9 build 24G830, x86_64, CPython 3.12.13
  (`3.12.13 (main, Jun 23 2026, 15:54:40) [Clang 22.1.3 ]`).
- Verdict: **pass**.

This is independent acceptance of C3-B4 only. It does not create, predict, or
verify a B4 manifest or checkpoint, does not authorize B5, and makes no
publication or conveyance decision.

## Candidate ordering, role separation, and scope

Git ancestry independently proves the accepted B3 checkpoint precedes candidate
`921f898`, which precedes verifier descendant `5c3427f`, which precedes the
accumulated-validation record at assigned HEAD `63af13b`. The exact candidate
tree recomputed to `0b0ea4529e6c2279cdd5b6b766377ea5e75a73e7`.
The verifier descendant adds only
`tests/verification/test_c3_b4_public_round15.py` and
`docs/reviews/c3-b4-verification-round-15.md`; the accumulated descendant adds
only `docs/reviews/c3-b4-accumulated-validation-round-8.md`. Thus no product,
fixture, limit, dependency, lock, workflow, or accepted B1-B3 byte changed after
candidate creation, while the exact hosted SHA contains the independent test
that binds and archives the immutable candidate.

The stable identities `c3-b4-public-implementor-1`,
`c3-b4-public-verifier-1`, root accumulated validator,
`c3-b4-public-acceptor-1`, and planned `c3-b4-manifest-verifier-1` remain
separate. The implementor authored no B1 authoritative expected output. Failed
and superseded B4 evidence is retained, and every product/test/workflow
correction precedes the round-15 candidate and its fresh verifier pass. No pass
was transferred across a corrected candidate.

The B3-checkpoint-to-candidate scope contains the assigned public decode module,
minimum private-host/resource integration, installed CLI, exact nine-file
package resource, package metadata and locks, documentation/licenses, B4 tests,
the existing macOS workflow, immutable review history, and root progress
entries. Static and history inspection found no browser/API decoder feature,
serial/live-capture route in decode, hardware/firmware action, dynamic decoder
selection, stacking, `.lac`, export, unsupported-platform claim, package build,
publication, B5 artifact, or Cycle 4 work. No `c3-b4.json` exists before this
acceptance.

## R17-R20 and public contract audit

- **R17 / stopping condition 11 library portion — pass.** The exported exact
  immutable class set and synchronous `decode_capture(capture, decoder_id,
  channels, options, limits=None)` surface consume a validated `CaptureResult`,
  canonicalize only the closed decoder mappings/options, delegate to the sole
  B2 subprocess host, retain version-1 declarations/records/rational time and
  canonical bytes, and reject caller limit weakening. The V001 unlisted-model
  leak and both V002 tightening gaps remain closed. Fresh exact-object,
  byte-vector, limit, installed-consumer, and delegation checks passed.
- **R18 / stopping condition 11 CLI portion — pass.** Installed `pico-la decode`
  retains the exact replay-or-CSV grammar, separate CSV physical identity and
  decoder mapping, explicit metadata/legacy-only inference rules, canonical
  compact sorted-key JSON plus LF, empty stdout on failure, bounded stderr, and
  exits 2/5/7. V003's CSV/replay taxonomy and V004's aggregate 8,192-byte
  diagnostic bound remain closed, including hostile ASCII and Unicode tokens.
- **R19 / conditions 13, 14, and 17 continuous portions — pass.** Decode imports
  and opens no serial or web surface; `aiohttp` remains web-extra-only and the
  base requirements remain exactly numpy and pyserial. Existing Cycle 1/2 CLI,
  replay/CSV, optional-web, assets, manifests, rollback, and macOS-only behavior
  are preserved. Static dependency/import/command scans and the final process
  inventory found no active C#/.NET, pythonnet, libsigrokdecode runtime,
  `sigrok-cli`, external decoder host, worker, pytest, Vite, Playwright, or
  characterization process. Historical source notice text is inert attribution,
  not a dependency or execution path.
- **R20 / stopping condition 12 — pass.** The clean hosted gate used the exact
  workflow SHA-256
  `12ff6bb2cd7e437b733f3aaeb9488c6926604572419468c60604f0dea1c6a990`,
  `macos-15-intel`, a full-history checkout, Node 22.14.0, and the pinned
  CPython 3.12.13 build archive SHA-256
  `a6bbea996c5f14eb55ab275889d2df45408deec504b4a7219d7b59c045b2555e`.
  Both clean Python environments, hash-locked installs, editable installs,
  `pip check`, Ruff, strict mypy, strict external consumer, the focused
  inherited-SIGTERM gate, full applicable non-hardware partition, installed
  help, frontend lint/typecheck/five tests/29-module build, asset drift,
  Chromium installation, and production browser regression completed
  successfully with no retry.

The applicable stopping-condition-16 ordering portion also passes: B4 has an
immutable candidate, distinct verifier, complete accumulated gate, this later
distinct acceptance, preserved corrections/findings, and no future manifest.
The five-checkpoint final outcome is deliberately not claimed at B4.

## Exact evidence and fresh commands

GitHub Actions run `33612103404`, job `100189420170`, concluded `success` on
`macos-15-intel` from `2026-09-02T09:04:22Z` through
`2026-09-02T09:16:10Z`, with exact head
`5c3427f0a176ba4db8f48b17521d9482f42ead8b`. The locally retained API response
under ignored `.tmp` independently reproduces that SHA, label, timestamps,
successful conclusion, and all successful step conclusions. The job's only
candidate delta is the independent round-15 verifier test/review described
above; immutable archive collection bound product candidate `921f898` to
1,479 selected of 1,517 collected cases with 38 transparent deselections.

The unchanged workflow partition has 15 ordered unique ignored stale B1
identity modules and 37 ordered unique selector strings representing 38 cases.
All 27 current B2/B3/B4 modules remain collected. The ignored and deselected
nodes are explicitly candidate-bound or superseded identity assertions, plus
the separately forced inherited-SIGTERM cases; no product module or mandatory
gate is hidden. Round-15 local validation reported 1,472 passes, seven
classified environment/optional-physical skips, and 38 deselections; the
hosted artifact paths made the hosted-required branches execute rather than
skip. Fresh independent verification reported seven fresh passes and a retained
1,034-pass direct sweep with one classified local absent-artifact skip and 37
direct-suite deselections.

This acceptance independently ran, with scratch only below repository-ignored
`.tmp/c3-b4-accept-r1`:

- the B4 implementation, V001-V004 verifier rounds 1-3, and round-15 portability
  modules: `70 passed in 20.75s`;
- `pip check`: no broken requirements; Ruff: all checks passed; strict mypy:
  31 production files and one external consumer file clean;
- strict schema validation for `c3-b1.json`, `c3-b2.json`, and `c3-b3.json`:
  all exit 0; fixture generation and characterization binding `--check`: both
  exit 0 with no drift;
- installed `pico-la --help` and `pico-la decode --help` from outside the source
  tree: exit 0 with the retained command and decode grammar;
- exact source-to-package-resource comparison: all nine installed snapshot
  bytes match the accepted B1 hashes, with no extra decoder resource; and
- `git diff --check`, final clean status, ancestry/tree/diff audits, scoped
  prohibited-runtime scan, and final process inventory: pass/empty as
  applicable.

The inherited manifest hashes remain C3-B1
`3710e7fde7523096b70607b3a7da217b1e71fdb46492b83b72861e3eaa48cfae`,
C3-B2 `2af5bfc21a5d888132fb4e468f543403e8948632ad9ab2dd11b8da854dd4a602`,
and C3-B3 `4d17a6f0a1401799f1d7c0c65870744aada26eb2a3b1b8d5c723d447ceb4fac1`.
The exact dev/web lock hashes remain
`4efa0b1d42e5162c7a20177d1dd5e78083d75a87d2405709e04b1bcbd4f33c53`
and `4b0feeadf92c0bcb9f2790cc62f6b6e2f7da8937f30ff50d0c9092fd003b8145`.
The public module/CLI hashes are
`d5cd8b843bf2bd847dc42a7fa550d5756615177d77e80912472d088b9e6519db`
and `c3acce4965faeff55c6177dfdfff840d7a2b9c65a6ae4b05b45f26974f1d3ddc`.

## Findings, legal boundary, and authorization

No open product, test, workflow, evidence-ordering, scope, authority, or
acceptance finding remains. C3B4-V001 through C3B4-V004 are closed by candidate
behavior and fresh checks. The exact decoder resources retain their original
headers and hashes; package metadata is `GPL-3.0-only` with the exact four
license/attribution files. This remains an engineering disposition, not a legal
conclusion. The B1 rights-holder/counsel limitation is controlling: shim
ownership, the historical MIT label, and combined-work treatment require
qualified-counsel or rights-holder confirmation before conveyance. No wheel,
sdist, upload, publication, conveyance, hardware, serial, live capture, or
external decoder runtime action occurred.

This pass authorizes only the root orchestrator to assemble the post-acceptance
C3-B4 manifest for this exact unchanged ordered record and submit it to the
distinct manifest verifier. It does not authorize a changed candidate, predict
manifest verification, claim a B4 checkpoint, launch B5, or begin Cycle 4.

`pass`

`APPROVAL_NEEDED`: none.
