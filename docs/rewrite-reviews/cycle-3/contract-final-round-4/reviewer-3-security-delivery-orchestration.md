# Cycle 3 contract final review — security, delivery, evidence, and orchestration

- Gate and round: `contract`; `contract-final-round-4`
- Reviewer: `/root/c3_final_r4_security` (security, delivery, and orchestration)
- Reviewed commit: `59390b75a406eebd6660d52381c68325ebd5fbdc`
- Reviewed tree: `cabb1fc44372be99f504384ff08985e169e07109`
- UTC timestamp: `2026-08-26T15:26:31Z`
- Verdict: `pass`

## Set-completeness declaration

I inspected every path in the exact six-file Step 8 contract-review set at the
commit and tree above, using commit content only and substituting no worktree
file:

1. `Software/LogicAnalyzer/PYTHON_REWRITE_IMPLEMENTATION_PLAN.md`;
2. `Software/LogicAnalyzerPy/docs/cycle-3-discovery-review.md`;
3. `CYCLE3_ORCHESTRATION.md`;
4. `CYCLE3_BATCH_EXECUTION.md`;
5. `docs/rewrite-reviews/cycle-3/README.md`; and
6. `docs/rewrite-reviews/cycle-3/ARCHITECTURAL_DOMAINS.md`.

I also inspected the repository instructions and parent review protocol, every
earlier contract-gate review record, the relevant Python dependency/lock and
hosted-CI metadata, accepted Cycle 1/Cycle 2 evidence interfaces, and preserved
C# project metadata as inert text. I did not execute, build, restore, install,
import, or load a decoder, C#, .NET, pythonnet, libsigrokdecode, `sigrok-cli`,
or another external/reference runtime, and none supplied expected output or
corroborating evidence.

## Findings and dispositions

No finding remains open. The reviewed candidate consistently freezes the
host-owned executable-code boundary, the exact pre-characterization cap
specification, the later independently qualified immutable actual runner,
mutually exclusive B1 fixture/runner authorship, parent-owned resource and
cleanup controls, the scoped production and non-production dependency ban, and
the directed manifest/checkpoint/completion evidence chain.

The repair for `C3-CFR3-R2-001` is complete at the security/orchestration seam.
The semantic-fixture candidate contains only the exact finite experiment-cap
set, schema, boundary cases, coverage, and independently derived rationale; its
verifier, accumulated gate, and acceptance identity perform static review of
those properties and explicitly neither test nor claim executable enforcement.
Only afterward may the distinct runner implementor create the immutable runner,
method, launch/import, cap-plumbing, and cleanup candidate. That exact later
candidate alone receives inert/hostile non-decoder enforcement proof by its own
verifier, complete accumulated validation, and its own acceptance pass before
the unchanged runner may execute an approved snapshot. No fixture-gate pass
transfers to runner enforcement.

## Prior-round reconciliation

### `contract-round-1` / `C3-CR1-R3-001` — resolved

- Resolving commit and evidence: resolution first appeared in
  `ed24f1badfff5200494cf2408e53c85cb44c0f02` and remains explicit in reviewed
  commit `59390b75a406eebd6660d52381c68325ebd5fbdc`. Discovery, orchestration,
  and the batch procedure separate B5 pre-manifest acceptance from later B5
  manifest assembly, independent manifest verification, closure-pending
  checkpointing, immutable completion-proof creation, closure audit, and
  terminal sealing.
- Disposition: resolved; B5 acceptance does not depend on a future B5 manifest,
  checkpoint, or closure artifact.

### `contract-round-1` / `C3-CR1-R3-002` — resolved

- Resolving commit and evidence: resolution first appeared in
  `ed24f1badfff5200494cf2408e53c85cb44c0f02` and remains unchanged in the
  reviewed candidate. The prohibition covers every active Cycle 3 Python
  product, development, test, fixture, characterization, verification,
  performance, acceptance, CI, bootstrap, lock, distribution, import,
  active-process, command, and evidence path. Preserved C# metadata may name a
  historical dependency only as inert inspection/rollback material and is
  never restored, built, installed, imported, loaded, executed, or used as
  evidence. Ambient availability is not use; invocation, import, linkage,
  resolution, active process, command reference, and evidence reliance are.
- Disposition: resolved; the ban includes development and test paths without
  falsely treating preserved inert source as an active dependency.

### `contract-round-1` / `C3-CR1-R3-003` — resolved

- Resolving commit and evidence: resolution first appeared in
  `ed24f1badfff5200494cf2408e53c85cb44c0f02` and remains in the reviewed
  candidate. Each batch assigns a manifest verifier distinct from the
  orchestrator/assembler to recompute artifact digests, validate the manifest
  schema, and create an immutable candidate- and manifest-bound verdict record.
- Disposition: resolved; the evidence producer does not independently verify
  its own manifest.

### `contract-final-round-1` / `C3-CFR1-R3-001` — resolved

- Resolving commit and evidence: commit
  `4d094b6d9a71e44387a386c85a586b1ecf5230fb` introduced a separate immutable
  actual-runner candidate containing the runner, method, exact launch/import
  configuration, all cap/cleanup plumbing, and probe sources. The reviewed
  candidate preserves independent inert/hostile non-decoder proof of every
  cap, digest/import boundary, termination, force-kill, close, reap, and
  post-failure path before unchanged snapshot execution.
- Disposition: resolved; the first snapshot cannot execute through an
  unqualified or later-modified runner.

### `contract-final-round-1` / `C3-CFR1-R3-002` — resolved

- Resolving commit and evidence: commit
  `4d094b6d9a71e44387a386c85a586b1ecf5230fb` froze the directed terminal
  sequence retained here: B5 manifest/verification-record commit;
  closure-pending checkpoint; immutable completion-proof candidate naming no
  future closure artifact; distinct committed closure verdict over unchanged
  inputs; and terminal non-audit-input seal committed last.
- Disposition: resolved; no audited completion artifact predicts or is amended
  to include its future verdict or seal.

### `contract-final-round-2` / `C3-CFR2-R3-001` — resolved

- Resolving commit and evidence: commit
  `6621d0881a102586f32de0b8cd2b1579a369a6cd` split B1 implementation ownership
  across a semantic-fixture implementor and a different pre-execution-runner
  implementor with reciprocal authorship exclusions. Each internal candidate
  has separate verifier and acceptance identities, and progress, assignment,
  handoff, final-candidate, manifest, checkpoint, and completion records retain
  the complete six-identity map without transferred passes.
- Disposition: resolved; the actual runner is assignable independently of the
  authoritative expected-output owner.

### `contract-final-round-2` / `C3-CFR2-R3-002` — resolved

- Resolving commit and evidence: commit
  `6621d0881a102586f32de0b8cd2b1579a369a6cd` aligned the complete contract set
  on one directed B1-B5 manifest protocol. A manifest may name its already
  assigned verifier but contains no future verdict, verification-record
  identity/path, manifest SHA-256, verification commands/results/findings, or
  self-dependent digest. The separate record names and hashes the unchanged
  manifest and exclusively owns those results and its verdict; both commit
  before a later checkpoint references them and the pass.
- Disposition: resolved; manifest verification neither predicts nor mutates the
  bytes it verifies.

### `contract-final-round-3` / `C3-CFR3-R2-001` — resolved

- Resolving commit and evidence: reviewed commit
  `59390b75a406eebd6660d52381c68325ebd5fbdc` changes discovery's B1 internal
  gate, orchestration's batch/role/validation clauses, the batch procedure's
  steps 3-8 and proof summaries, and both review-framework files. They now
  consistently limit the semantic-fixture gate to static review of cap-set
  completeness, exact finite values, schema, boundaries, coverage, and
  independent derivation, with executable enforcement absent and unclaimed.
  Every executable cap, digest/import, termination, close, and reap proof is
  exclusive to the later immutable actual-runner candidate and its separate
  verifier and acceptance identity.
- Disposition: resolved; the fixture gate no longer requests impossible proof
  against a runner that does not yet exist, and no enforcement pass can be
  transferred from a placeholder or the fixture owner.

## Cross-domain reconciliation

The round-4 correction does not reopen Reviewer 1's exact experiment-envelope,
integer-`0xFF` optional-pin, closed option-matrix, independent-fixture, import,
or provenance resolutions. It strengthens their ownership boundary by keeping
all executable enforcement outside the fixture candidate and preserving the
separate unchanged-runner gate.

It also does not reopen Reviewer 2's explicit CSV metadata or version-1
result/error schema resolutions. The characterization runner remains private
and non-installed; public library and CLI behavior still delegates only to the
later accepted B2 host. Capture identity, typed results, canonical bytes,
failure classes, exits, and the browser/API exclusion are unchanged.

The lower-authority roadmap retains historical and later-parity language, but
it expressly supplies context rather than Cycle 3 execution authority. The
approved discovery decisions and higher-order governing contracts prohibit C#
execution for every Cycle 3 purpose and prohibit .NET, pythonnet,
libsigrokdecode, `sigrok-cli`, and external/reference hosts from production,
development, test, fixture, verification, performance, acceptance, CI, and
evidence paths.

All earlier contract findings have durable dispositions at the reviewed commit,
the named commit and tree agree, and no pass was transferred across a candidate
or round. No new security, delivery, orchestration, evidence-integrity, or
cross-domain finding was identified. Verdict: `pass`.
